/**
 * Centralized identity for the in-app AI assistant. Change `AI_ASSISTANT_NAME`
 * here to rebrand the assistant across every user-facing string (toasts, button
 * labels, server error messages, and the moon icon's accessible label).
 *
 * This is a plain constant with no client/server directives, so it is safe to
 * import from both Server Actions and client components.
 */
export const AI_ASSISTANT_NAME = "SHIRA";
